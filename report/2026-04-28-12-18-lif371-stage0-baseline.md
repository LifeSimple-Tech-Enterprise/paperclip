# LIF-371 Stage 0 — Pre-FSM Wake-Event Baseline

**Captured:** 2026-04-28T12:18:18.991Z (≈40 min after restart at 14:07:50 CEST loaded LIF-377 commit `911391e5`)
**Window:** 7-day rolling (`2026-04-21T12:18:18.991Z → 2026-04-28T12:18:18.991Z`)
**Source:** `GET /api/companies/:companyId/wake-events/baseline`
**Purpose:** held as the "pre-FSM" reference per Stage 0 scope. Stage 1 (LIF-373, Wake FSM) will be measured against this snapshot.

## Headline numbers

- `totalWakes`: **1189**
- `byReasonAndTransition` rows: 20
- `ctxFieldUsage`: 4 wakes used `ctx.context` (the gotcha-`adapter_ctx_context_vs_config` path), 1185 null (pre-restart historical, not instrumented).
- `suppressed`: 0 rows.

## Post-restart capture rows (the only ones with populated transition fields)

These are the four wakes the restarted server has observed since `14:07:50 CEST`. They already exhibit the patterns Stage 1 is meant to eliminate:

| wakeReason                  | priorStatus | postCheckoutStatus | count | gotcha-key                 |
| --------------------------- | ----------- | ------------------ | ----- | -------------------------- |
| `issue_execution_promoted`  | (null)      | `done`             | 1     | (terminal, expected)       |
| `issue_children_completed`  | `blocked`   | `in_progress`      | 1     | `done_status_flip`         |
| `issue_continuation_needed` | (null)      | `in_progress`      | 1     | `continuation_hot_loop`    |
| `process_lost_retry`        | (null)      | `in_progress`      | 1     | (recovery, expected)       |

The single `issue_children_completed: blocked → in_progress` row is the canonical pain point: an issue I parked as `blocked` got silently flipped to `in_progress` on a child-completion wake, which is exactly what `gotcha_paperclip_done_status_flip` documents and what Stage 1's typed FSM is intended to make impossible (or at least loud).

## Pre-restart historical rows (null transition fields, kept for volume reference)

20 distinct `wakeReason` values appear in the 7-day window. Counts of note (transition fields are null because the columns didn't exist before LIF-377):

- `issue_assigned`: 197
- `issue_children_completed` (no transition data): 45
- `issue_reopened_via_comment`: 16
- `issue_dependencies_blocked`: 5
- `missing_issue_comment`: 4
- One free-form reason string: `"CEO-recovery-from-LIF-288: clear error state after 2026-04-25 rate-limit cascade per Lead_Engineer diagnosis"` — exemplifies the *no schema for `wakeReason`* problem Stage 1 fixes by introducing a typed enum.

## How to reproduce

```bash
curl -s -H "Authorization: Bearer $PAPERCLIP_API_KEY" \
  "http://127.0.0.1:3100/api/companies/11eb63eb-c454-467e-b021-fffd17747395/wake-events/baseline"
```

## Stage gate

Per LIF-371 plan rev 2 board ruling (comment `02684265`): LIF-372 ships to `done` only after one full real-work cycle (non-meta product issue) runs Drafter → Critique → Lead → close cleanly with this dashboard producing the corresponding row. LIF-377 (refactor) and LIF-378 (ops) are meta and do not satisfy the gate.
