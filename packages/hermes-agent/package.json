{
  "name": "@paperclipai/hermes-agent",
  "version": "0.0.1",
  "private": true,
  "license": "MIT",
  "description": "HermesAgent process-adapter entrypoint: converts a Paperclip issue + comment into a structured intent JSON via local Ollama, validates against the V1 action registry, and either dispatches or rejects.",
  "type": "module",
  "exports": {
    ".": "./src/index.ts",
    "./intent": "./src/intent.ts"
  },
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "zod": "^3.24.2"
  },
  "devDependencies": {
    "@types/node": "^25.0.0",
    "typescript": "^5.9.3"
  }
}
